<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.StoryApp" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="colorPrimary">@color/blueOcean</item>
        <item name="colorOnPrimary">@color/white</item>
        <item name="android:statusBarColor">?attr/colorOnPrimary</item>
        <item name="android:windowLightStatusBar">true</item>
    </style>
    <style name="Theme.StoryApp" parent="Base.Theme.StoryApp"/>
    <style name="TextInputLayoutCustom" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
        <item name="boxBackgroundMode">outline</item>
        <item name="boxStrokeColor">@color/blueOcean</item> <!-- Ganti dengan warna stroke yang diinginkan -->
        <item name="boxCornerRadiusTopStart">8dp</item> <!-- Ganti dengan radius yang diinginkan -->
        <item name="boxCornerRadiusTopEnd">8dp</item> <!-- Ganti dengan radius yang diinginkan -->
        <item name="boxCornerRadiusBottomEnd">8dp</item> <!-- Ganti dengan radius yang diinginkan -->
        <item name="boxCornerRadiusBottomStart">8dp</item>
    </style>
</resources>